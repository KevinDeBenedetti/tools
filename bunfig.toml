# Tailwind v4 for the web UI. Bun's dev server bundles the CSS imported by
# src/web/app/index.html; this plugin is what makes `@import "tailwindcss"`
# inside it resolve and generate utilities.
[serve.static]
plugins = ["bun-plugin-tailwind"]
