/**
 * Centralized default values used across the Copilot tools.
 * Update these values to change defaults globally.
 */

/** Default model for Copilot API calls */
export const DEFAULT_MODEL = "gpt-5-mini";
